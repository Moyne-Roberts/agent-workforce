---
phase: quick-260319-cl3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [RESTRUCTURE]
must_haves:
  truths:
    - "orq-agent/ directory no longer exists in the repo"
    - "PROJECT.md reflects 'Agent Workforce' identity focused on web app"
    - "Auth constraint says email/password primary, M365 SSO when Azure is ready"
    - "ROADMAP.md references to CLI skills note they live in orqai-agent-pipeline repo"
    - "STATE.md blockers updated to reflect Azure is deferred not blocking"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated project identity, auth constraint, scope"
    - path: ".planning/ROADMAP.md"
      provides: "Updated overview, phase descriptions, CLI references"
    - path: ".planning/STATE.md"
      provides: "Updated decisions, blockers reflecting new auth strategy"
  key_links: []
---

<objective>
Restructure the agent-workforce repo from a dual CLI+web project to a frontend-first web app. Remove the orq-agent/ CLI skills directory (lives in the original orqai-agent-pipeline repo), update auth strategy from M365-only to email/password primary with SSO as future swap-in, and update all planning docs to reflect the "Agent Workforce" identity.

Purpose: Unblock V3.0 development by removing Azure AD dependency and clarifying project scope.
Output: Clean repo with updated PROJECT.md, ROADMAP.md, STATE.md reflecting frontend-first identity.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orq-agent/ directory and update top-level files</name>
  <files>orq-agent/, install.sh, README.md, CHANGELOG.md, VERSION</files>
  <action>
1. Delete the entire orq-agent/ directory: `rm -rf orq-agent/`
2. Delete install.sh (CLI skill installer -- belongs in orqai-agent-pipeline repo, not here)
3. Delete VERSION file (tracks CLI skill version, not web app version)
4. Delete CHANGELOG.md (tracks CLI skill changelog -- web app will get its own if needed)
5. Update README.md to reflect "Agent Workforce" as a Next.js web application:
   - Title: "Agent Workforce"
   - Description: Browser-based interface for creating, deploying, testing, and iterating agent swarms on Orq.ai
   - Remove all references to Claude Code skill, `/orq-agent` commands, install script
   - Note that CLI skills live separately in the orqai-agent-pipeline repo
   - Point to web/ directory as the main application
   - Keep it brief -- 10-20 lines max
  </action>
  <verify>
    <automated>test ! -d orq-agent/ && test ! -f install.sh && test ! -f VERSION && test ! -f CHANGELOG.md && echo "PASS: CLI artifacts removed" || echo "FAIL"</automated>
  </verify>
  <done>orq-agent/ directory, install.sh, VERSION, and CHANGELOG.md are deleted. README.md describes Agent Workforce as a web app.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md, ROADMAP.md, and STATE.md for frontend-first identity</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
**PROJECT.md changes:**
1. Title: "Orq Agent Designer" -> "Agent Workforce"
2. "What This Is" section: Remove "Also available as a Claude Code skill (`/orq-agent`) for technical users." Focus on web app only.
3. Core Value: Keep as-is (already web-focused).
4. Active Requirements: Change "M365 SSO authentication (Azure AD) -- Moyne Roberts employees only" to "Authentication (email/password primary, M365 SSO when Azure AD is ready)"
5. Constraints section:
   - Auth constraint: "Must use M365 SSO (Azure AD)" -> "Email/password auth primary. M365 SSO as additional provider when Azure AD tenant setup is complete."
   - Remove "Backward compat" constraint about Claude Code skill continuing to work alongside web app (no longer relevant -- CLI is separate repo)
   - Remove SDK pins constraint (those belong to the CLI skill, not the web app)
6. Context section:
   - Remove "Codebase: 10,628 lines across orq-agent/" line
   - Update "Distribution model" to remove dual distribution mention. Simply: "Web app for all users. CLI skills available separately in orqai-agent-pipeline repo."
   - Remove "V2.0 pipeline" context line (CLI-specific detail)
7. Current Milestone section: Update "Foundation & Auth" target feature from "M365 SSO" to "email/password + future SSO"
8. Key Decisions table:
   - Update "Next.js + Supabase + Vercel" rationale to mention email/password auth primary
   - Remove or update "Claude Code skill distribution via GitHub" decision (mark as "Moved to orqai-agent-pipeline")
   - Keep all other decisions as-is

**ROADMAP.md changes:**
1. Title: "Orq Agent Designer" -> "Agent Workforce"
2. Overview: Remove "Also available as a Claude Code skill" mentions. Focus on web app.
3. Phase 34 description: "M365 SSO with tenant restriction" -> "Supabase auth (email/password, M365 SSO future)"
4. Phase 34 Success Criteria:
   - Criterion 1: "User can sign in with their Moyne Roberts M365 account" -> "User can sign in with email/password and reach the app dashboard"
   - Criterion 2: Remove "A personal Microsoft account is rejected at login" (not applicable to email/password). Replace with: "Auth is configured so M365 SSO can be added as an additional provider without code changes"
5. V3.0 milestone description: Remove M365 SSO mention, say "authentication" generically
6. Plan 34-01 description: Already says "M365 SSO + email/password" -- update to "email/password auth (M365 SSO future)"
7. Keep references to "orq-agent" pipeline prompts where they describe runtime fetching from GitHub (this is about the prompts, not the CLI skill)

**STATE.md changes:**
1. Update "Core value" in Project Reference to remove any CLI mention
2. Decisions section: Add new decision: "Email/password auth primary while Azure AD setup pending -- M365 SSO swap-in when ready, no throwaway code"
3. Decisions section: Add: "orq-agent/ CLI skills separated to orqai-agent-pipeline repo -- this repo is web app only"
4. Blockers section: Update "Azure AD tenant misconfiguration" blocker to: "Azure AD tenant setup pending -- using email/password auth as workaround, SSO deferred to future addition"
  </action>
  <verify>
    <automated>grep -q "Agent Workforce" .planning/PROJECT.md && grep -q "email/password" .planning/PROJECT.md && grep -q "Agent Workforce" .planning/ROADMAP.md && grep -q "email/password" .planning/STATE.md && echo "PASS: Docs updated" || echo "FAIL"</automated>
  </verify>
  <done>All three planning docs reflect "Agent Workforce" identity with email/password as primary auth. No references to orq-agent/ as part of this repo. Azure SSO documented as future addition, not blocker.</done>
</task>

</tasks>

<verification>
1. `orq-agent/` directory does not exist
2. `install.sh`, `VERSION`, `CHANGELOG.md` do not exist
3. PROJECT.md title is "Agent Workforce", auth constraint says email/password
4. ROADMAP.md Phase 34 describes email/password auth, not M365-only
5. STATE.md has new decisions about auth workaround and skills separation
6. No broken references to orq-agent/ as if it were local
</verification>

<success_criteria>
- Repo is cleanly frontend-only: no CLI skill artifacts remain
- Planning docs consistently describe "Agent Workforce" as a web app with email/password auth
- Azure SSO is documented as a future swap-in, not a blocker
- All V3.0 phase descriptions are updated to reflect email/password primary auth
</success_criteria>

<output>
After completion, create `.planning/quick/260319-cl3-restructure-project-for-frontend-first-a/260319-cl3-SUMMARY.md`
</output>
