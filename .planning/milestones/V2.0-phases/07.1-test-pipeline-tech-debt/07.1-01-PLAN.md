---
phase: 07.1-test-pipeline-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/references/orqai-api-endpoints.md
  - orq-agent/agents/tester.md
  - orq-agent/SKILL.md
  - orq-agent/templates/deploy-log.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "orqai-api-endpoints.md contains an SDK Method Mapping section that maps agents.responses.create() to POST /v2/agents/{agent_id}/execute"
    - "@orq-ai/evaluators@^1.1.0 is declared with version pin in SKILL.md alongside existing @orq-ai/node and @orq-ai/evaluatorq packages"
    - "deploy-log.json template file no longer exists in orq-agent/templates/"
    - "SKILL.md template table no longer references deploy-log.json"
    - "tester.md Step 7.1 includes a REST fallback note for agent invocation showing the raw endpoint, headers, and body"
  artifacts:
    - path: "orq-agent/references/orqai-api-endpoints.md"
      provides: "SDK-to-REST cross-reference mapping"
      contains: "agents.responses.create"
    - path: "orq-agent/agents/tester.md"
      provides: "REST fallback documentation for agent invocation"
      contains: "POST /v2/agents"
    - path: "orq-agent/SKILL.md"
      provides: "V2.0 Runtime Dependencies table and cleaned template table"
      contains: "@orq-ai/evaluators"
  key_links:
    - from: "orq-agent/agents/tester.md"
      to: "orq-agent/references/orqai-api-endpoints.md"
      via: "SDK method mapping cross-reference"
      pattern: "agents.responses.create.*POST /v2/agents"
    - from: "orq-agent/SKILL.md"
      to: "orq-agent/templates/"
      via: "Template table listing"
      pattern: "test-results.json|iteration-log.json"
---

<objective>
Resolve three integration gaps identified in the V2.0 milestone audit: (1) document the SDK-to-REST mapping for agent invocation so the tester's REST fallback path is fully specified, (2) declare the undeclared @orq-ai/evaluators package with version pin, and (3) remove the orphaned deploy-log.json template that does not match the live markdown format.

Purpose: Clean up tech debt before Phase 9 (Guardrails) depends on deploy/test infrastructure. These are purely documentation and file-system edits -- zero custom code.
Output: Updated orqai-api-endpoints.md, tester.md, SKILL.md; deleted deploy-log.json
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.1-test-pipeline-tech-debt/07.1-RESEARCH.md
@orq-agent/references/orqai-api-endpoints.md
@orq-agent/agents/tester.md
@orq-agent/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SDK-to-REST mapping to API endpoints reference and REST fallback note to tester</name>
  <files>orq-agent/references/orqai-api-endpoints.md, orq-agent/agents/tester.md</files>
  <action>
**File 1: orq-agent/references/orqai-api-endpoints.md**

Add an "SDK Method Mapping" section at the end of the file (before the closing content, after the "Usage Notes" section). The section should contain:

```markdown
## SDK Method Mapping

Key SDK methods used by subagents and their REST API equivalents. Use these when the `@orq-ai/node` SDK is unavailable or for manual debugging.

| SDK Method | REST Equivalent | Used By |
|------------|----------------|---------|
| `client.agents.responses.create({ agent_id, messages })` | `POST /v2/agents/{agent_id}/execute` with `{ messages: [...] }` body | tester.md (Step 7.1) |
| `client.datasets.create({ name, ... })` | `POST /v2/datasets` | tester.md (Step 5) |
| `client.datasets.addRows({ dataset_id, rows })` | `POST /v2/datasets/{dataset_id}/rows` | tester.md (Step 5) |
```

**File 2: orq-agent/agents/tester.md**

In Step 7.1 (the agent invocation step, around line 431-435 where `agents.responses.create()` is documented), add the following REST fallback note immediately after the "Agent invocation details" bullet list (after line 435, before Step 7.2):

```markdown
**REST fallback for agent invocation:**
If the `@orq-ai/node` SDK is unavailable, agent invocation maps to:
- Endpoint: `POST /v2/agents/{orqai_id}/execute`
- Headers: `Authorization: Bearer $ORQ_API_KEY`, `Content-Type: application/json`
- Body: `{ "messages": [{ "role": "user", "content": "{input text}" }] }`
- Response: Extract agent output from response body
- See `orqai-api-endpoints.md` SDK Method Mapping table for full cross-reference
```

Do NOT create a parallel REST code path. This is documentation only -- the SDK already handles the REST call internally. The note is for when the SDK itself is unavailable or for debugging.
  </action>
  <verify>
    <automated>grep -c "SDK Method Mapping" orq-agent/references/orqai-api-endpoints.md && grep -c "REST fallback for agent invocation" orq-agent/agents/tester.md</automated>
  </verify>
  <done>orqai-api-endpoints.md has an "SDK Method Mapping" section with agents.responses.create() mapped to POST /v2/agents/{agent_id}/execute. tester.md Step 7.1 has a REST fallback note showing the raw endpoint, headers, body, and cross-reference to orqai-api-endpoints.md.</done>
</task>

<task type="auto">
  <name>Task 2: Declare @orq-ai/evaluators package and remove orphaned deploy-log.json</name>
  <files>orq-agent/SKILL.md, orq-agent/templates/deploy-log.json</files>
  <action>
**File 1: orq-agent/SKILL.md — Add V2.0 Runtime Dependencies section**

Add a "V2.0 Runtime Dependencies" section after the "Capability Tiers" section and before the "Templates" section. Content:

```markdown
## V2.0 Runtime Dependencies

Packages required by V2.0 subagents at runtime. Not installed by `install.sh` (which only copies files) — subagents ensure availability at execution time.

| Package | Version | Purpose | Tier Required |
|---------|---------|---------|---------------|
| `@orq-ai/node` | `^3.14.45` | Orq.ai SDK (agent CRUD, dataset ops, agent invocation) | deploy+ |
| `@orq-ai/evaluatorq` | `^1.1.0` | Experiment execution framework (Effect-based parallel evaluations) | test+ |
| `@orq-ai/evaluators` | `^1.1.0` | Local evaluator scorers (peer dep of evaluatorq — cosine similarity, threshold evaluators) | test+ |

> **Note:** `@orq-ai/evaluatorq` also declares peer dependencies on `@opentelemetry/*` packages for tracing. These are optional — evaluatorq works without them but npm may issue warnings.
```

**File 2: orq-agent/SKILL.md — Remove deploy-log.json from template table**

In the "Templates" section, remove the row:
```
| `deploy-log.json` | V2.0 deployment audit log template (JSON) |
```

Replace it with nothing (just remove the row). The remaining template entries should be: agent-spec.md, orchestration.md, dataset.md, readme.md, tools.md, test-results.json, iteration-log.json.

Also in the "Directory Structure" section near the top, update the line:
```
    deploy-log.json              # Phase 5: V2.0 deployment audit template (JSON)
```
Remove this line from the directory tree.

**File 3: orq-agent/templates/deploy-log.json — DELETE**

Delete this file entirely. It is orphaned — the deployer generates `deploy-log.md` directly from inline instructions in deployer.md Phase 5. The JSON template was a placeholder created during Phase 5 foundation before the deployer was built. It is missing `unchanged` status support and uses JSON format while the live format is markdown.

Use `git rm orq-agent/templates/deploy-log.json` to remove and stage the deletion.
  </action>
  <verify>
    <automated>grep -c "@orq-ai/evaluators" orq-agent/SKILL.md && test ! -f orq-agent/templates/deploy-log.json && echo "deploy-log.json deleted" && grep -cv "deploy-log.json" orq-agent/SKILL.md > /dev/null && echo "SKILL.md cleaned"</automated>
  </verify>
  <done>SKILL.md contains a "V2.0 Runtime Dependencies" table declaring all 3 Orq.ai packages with version pins. deploy-log.json is deleted from orq-agent/templates/. SKILL.md template table and directory structure no longer reference deploy-log.json.</done>
</task>

</tasks>

<verification>
1. `grep "SDK Method Mapping" orq-agent/references/orqai-api-endpoints.md` — section exists
2. `grep "agents.responses.create" orq-agent/references/orqai-api-endpoints.md` — SDK method mapped
3. `grep "REST fallback for agent invocation" orq-agent/agents/tester.md` — fallback note present
4. `grep "@orq-ai/evaluators" orq-agent/SKILL.md` — package declared
5. `grep "\\^1.1.0" orq-agent/SKILL.md` — version pinned
6. `test ! -f orq-agent/templates/deploy-log.json` — orphaned template removed
7. `grep -c "deploy-log.json" orq-agent/SKILL.md` — returns 0 (no stale references)
</verification>

<success_criteria>
- orqai-api-endpoints.md has SDK Method Mapping section with agents.responses.create() to POST /v2/agents/{agent_id}/execute mapping
- tester.md Step 7.1 includes REST fallback note with endpoint, headers, body, and cross-reference
- SKILL.md declares @orq-ai/evaluators@^1.1.0 in a V2.0 Runtime Dependencies table
- deploy-log.json no longer exists in orq-agent/templates/
- SKILL.md template table and directory tree have no deploy-log.json references
- All changes are documentation/file-system edits only — no new code paths
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-test-pipeline-tech-debt/07.1-01-SUMMARY.md`
</output>
