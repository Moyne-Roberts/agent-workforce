---
phase: 11-flag-conventions-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/commands/harden.md
  - orq-agent/commands/test.md
  - orq-agent/commands/iterate.md
  - orq-agent/commands/orq-agent.md
  - orq-agent/agents/orchestration-generator.md
  - orq-agent/SKILL.md
  - .planning/STATE.md
autonomous: true
requirements:
  - GUARD-03
must_haves:
  truths:
    - "harden.md uses --agent flag syntax (not positional [agent-key]) matching deploy/test/iterate convention"
    - "test.md and iterate.md no longer mention positional argument or backward compatibility"
    - "TOOLS.md is passed to Wave 3 dataset-generator and readme-generator in orq-agent.md"
    - "agentic-patterns.md is included in orchestration-generator files_to_read"
    - "Orchestrator step numbering is sequential: Step 5 (Tool Resolver), Step 6 (Pipeline), Step 7 (Summary)"
    - "No stale cross-references to Step 5.5 exist in orq-agent.md"
    - "STATE.md no longer references positional arg backward compatibility"
  artifacts:
    - path: "orq-agent/commands/harden.md"
      provides: "--agent flag convention with --all flag support"
      contains: "--agent agent-key"
    - path: "orq-agent/commands/test.md"
      provides: "Clean --agent flag convention without positional fallback"
    - path: "orq-agent/commands/iterate.md"
      provides: "Clean --agent flag convention without positional fallback"
    - path: "orq-agent/commands/orq-agent.md"
      provides: "Sequential step numbering and TOOLS.md in Wave 3 inputs"
    - path: "orq-agent/agents/orchestration-generator.md"
      provides: "Complete files_to_read including agentic-patterns.md"
  key_links:
    - from: "orq-agent/SKILL.md"
      to: "orq-agent/commands/harden.md"
      via: "Command Flags table"
      pattern: "--agent.*harden"
    - from: "orq-agent/commands/orq-agent.md"
      to: "orq-agent/agents/dataset-generator.md"
      via: "Wave 3 input passing"
      pattern: "TOOLS.md"
---

<objective>
Align CLI flag conventions across all orq-agent commands and resolve carried-forward tech debt from v0.3.

Purpose: Complete the GUARD-03 integration by ensuring all commands (deploy, test, iterate, harden) use consistent `--agent` flag syntax for per-agent workflows. Fix orchestrator step numbering, add missing file references, and sweep for consistency issues.

Output: All command files use `--agent` flag convention, orchestrator has clean sequential step numbering, subagent files_to_read are complete, and STATE.md reflects current conventions.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-flag-conventions-tech-debt/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix flag conventions, step numbering, and files_to_read references</name>
  <files>orq-agent/commands/harden.md, orq-agent/commands/test.md, orq-agent/commands/iterate.md, orq-agent/commands/orq-agent.md, orq-agent/agents/orchestration-generator.md</files>
  <action>
Apply all four targeted fixes identified in 11-RESEARCH.md. Use deploy.md as the reference implementation for flag conventions.

**1. Fix harden.md flag convention (line ~115):**
- Change `**Command format:** /orq-agent:harden [agent-key]` to `**Command format:** /orq-agent:harden [--agent agent-key] [--all]`
- Replace the positional argument parsing with flag-based parsing matching deploy.md pattern:
  - If `--agent agent-key` is provided: filter hardening to that single agent
  - If `--all` is provided: explicitly harden all agents in the swarm
  - If no flags: harden all agents in the swarm (default behavior, same as `--all`)
- Remove any references to positional `[agent-key]` argument

**2. Fix test.md flag convention (lines ~121-128):**
- Remove the line about positional `agent-key` backward compatibility (line ~124: "If positional `agent-key` is provided (backward compatible): same as `--agent agent-key`")
- Remove the backward compatibility Note block (lines ~126-128)
- Update the "no flags" line to remove "and no positional argument" phrasing
- Keep `--agent` and `--all` flag descriptions intact

**3. Fix iterate.md flag convention (lines ~119-127):**
- Apply identical changes as test.md — remove positional arg support and backward compat notes
- Keep `--agent` and `--all` flag descriptions intact

**4. Fix orq-agent.md step renumbering (8 locations from research):**
- Line ~246: "Store the researcher decision (RUN or SKIP) for use in Step 5 (generation pipeline)" → change "Step 5" to "Step 6"
- Line ~365: `## Step 5.5: Run Tool Resolver` → `## Step 5: Run Tool Resolver`
- Line ~398: `## Step 5: Execute Generation Pipeline` → `## Step 6: Execute Generation Pipeline`
- Line ~405: "include `tool_resolver` from Step 5.5 if it ran" → "from Step 5 if it ran"
- Line ~446: "if Step 5.5 failed" → "if Step 5 failed"
- Line ~459: "if Step 5.5 failed" → "if Step 5 failed"
- Line ~498: "if Step 5.5 failed" → "if Step 5 failed"
- Line ~588: `## Step 6: Final Summary` → `## Step 7: Final Summary`
- Use plain "Step N" format (no parenthetical descriptions)
- IMPORTANT: Do NOT change Step 5.5 in hardener.md — that is a different file with its own internal numbering

**5. Fix orq-agent.md Wave 3 TOOLS.md (lines ~549-554):**
- In the Dataset Generator section of Wave 3, change "Pass three file paths per agent" to "Pass four file paths per agent" and add item 4: `TOOLS.md: {OUTPUT_DIR}/[swarm-name]/TOOLS.md` (or note "Tool resolution unavailable" if Step 5 failed)
- Apply the same addition to the README Generator input list in Wave 3

**6. Fix orchestration-generator.md files_to_read (lines ~8-12):**
- Add `- orq-agent/references/agentic-patterns.md` to the `<files_to_read>` block, between `orqai-agent-fields.md` and `orchestration.md` (the template)
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/claude-code-prompt-agent && grep -c "agent-key\]" orq-agent/commands/harden.md | grep -q "0" && grep -c "positional" orq-agent/commands/test.md | grep -q "0" && grep -c "positional" orq-agent/commands/iterate.md | grep -q "0" && grep -c "5\.5" orq-agent/commands/orq-agent.md | grep -q "0" && grep -c "agentic-patterns" orq-agent/agents/orchestration-generator.md | grep -qv "0" && grep -c "TOOLS.md" orq-agent/commands/orq-agent.md | grep -qv "0" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
- harden.md uses `--agent agent-key` flag with `--all` support (no positional arg)
- test.md has no positional argument references or backward compatibility notes
- iterate.md has no positional argument references or backward compatibility notes
- orq-agent.md steps are sequential: 0, 1, 2, 3, 4, 5 (Tool Resolver), 6 (Pipeline), 7 (Summary)
- orq-agent.md has zero occurrences of "5.5"
- orq-agent.md Wave 3 passes TOOLS.md to dataset-generator and readme-generator
- orchestration-generator.md files_to_read includes agentic-patterns.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Consistency sweep and STATE.md update</name>
  <files>orq-agent/SKILL.md, .planning/STATE.md</files>
  <action>
**1. Verify SKILL.md Command Flags table (lines ~98-106):**
- Read the table and confirm it shows `--agent {key}` for deploy, test, iterate, harden
- Confirm it shows `--all` for test, iterate, harden
- If the table already matches (research suggests it does), no changes needed
- If any discrepancies, fix to match the now-updated command files
- Also check command usage examples elsewhere in SKILL.md for stale positional arg references

**2. Update STATE.md stale decision (line ~84):**
- Find the decision: "Test/iterate support both positional arg and --agent flag for backward compatibility"
- Replace with: "All commands use `--agent` flag exclusively for per-agent workflows (no positional args)"

**3. Full consistency sweep of orq-agent/ directory:**
Audit all files in the orq-agent/ directory for consistency issues. The targeted fixes in Task 1 handle the known issues. This sweep catches anything the research may have missed.

Search for and fix:
- Any remaining references to positional `agent-key` argument patterns across ALL files (commands/, agents/, references/, templates/)
- Any references to old step numbers (Step 5.5, or Step 5 meaning Pipeline, Step 6 meaning Summary) in files OTHER than orq-agent.md
- Any stale backward compatibility notes
- help.md command examples — verify they show `--agent` flag syntax
- Any other flag convention inconsistencies

For each finding: fix it in place. Do NOT create a separate findings report.

**Priority order:** Fix anything that would cause incorrect agent behavior first, then cosmetic/documentation issues.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/claude-code-prompt-agent && grep -r "positional" orq-agent/ --include="*.md" | grep -v "RESEARCH\|CONTEXT" | wc -l | grep -q "0" && grep -r "backward compat" orq-agent/ --include="*.md" | grep -v "RESEARCH\|CONTEXT" | wc -l | grep -q "0" && echo "SWEEP CLEAN"</automated>
  </verify>
  <done>
- SKILL.md Command Flags table matches the updated command files
- STATE.md decision updated to reflect flag-only convention
- No stale positional argument references remain in orq-agent/ directory
- No stale backward compatibility notes remain in orq-agent/ directory
- help.md (if it contains command examples) shows --agent flag syntax
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run a comprehensive verification:

1. `grep -rn "positional" orq-agent/ --include="*.md"` — should return zero results (excluding planning files)
2. `grep -rn "backward compat" orq-agent/ --include="*.md"` — should return zero results (excluding planning files)
3. `grep -n "5\.5" orq-agent/commands/orq-agent.md` — should return zero results
4. `grep -n "agent-key\]" orq-agent/commands/harden.md` — should return zero results (no positional bracket syntax)
5. `grep -n "--agent" orq-agent/commands/harden.md` — should return at least one result
6. `grep -n "agentic-patterns" orq-agent/agents/orchestration-generator.md` — should return at least one result
7. `grep -n "TOOLS.md" orq-agent/commands/orq-agent.md` — should find TOOLS.md in Wave 3 section
8. Verify step headings in orq-agent.md are sequential: Step 0 through Step 7
</verification>

<success_criteria>
1. harden.md Step 3 uses `--agent` flag (not positional argument), matching deploy/test/iterate commands and SKILL.md documentation
2. TOOLS.md is included in Wave 3 `files_to_read` for dataset-gen and readme-gen subagents
3. agentic-patterns.md is included in orchestration-generator `files_to_read`
4. Step 5.5/5 numbering inversion in orchestrator is corrected to sequential 5/6/7
5. No stale positional argument or backward compatibility references remain anywhere in orq-agent/
6. STATE.md reflects current flag-only convention
</success_criteria>

<output>
After completion, create `.planning/phases/11-flag-conventions-tech-debt/11-01-SUMMARY.md`
</output>
