---
phase: 10-fix-holdout-dataset-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orq-agent/templates/test-results.json
  - orq-agent/agents/tester.md
  - orq-agent/agents/iterator.md
autonomous: true
requirements: [ITER-05]

must_haves:
  truths:
    - "test-results.json template per_agent_datasets[] entries include train_dataset_id, test_dataset_id, and holdout_dataset_id fields alongside existing dataset_id"
    - "tester.md Phase 5.3 instructs writing per-split dataset IDs to per_agent_datasets[] in test-results.json"
    - "tester.md Output Format section includes per-split dataset ID fields"
    - "iterator.md Phase 7 Step 7.2 reads holdout_dataset_id from dataset.per_agent_datasets[] array lookup (not dot-notation keyed object)"
    - "iterator.md Phase 7 Step 7.1 uses correct array-based path for holdout dataset IDs"
    - "iterator.md Phase 9 step labels read Step 9.1, Step 9.2, Step 9.3 (not stale Step 7.x)"
    - "iterator.md backward compatibility warning exists for old test-results.json files missing per-split IDs"
  artifacts:
    - path: "orq-agent/templates/test-results.json"
      provides: "Schema with per-split dataset ID fields"
      contains: "holdout_dataset_id"
    - path: "orq-agent/agents/tester.md"
      provides: "Instructions to write per-split IDs + Output Format with per-split fields"
      contains: "holdout_dataset_id"
    - path: "orq-agent/agents/iterator.md"
      provides: "Correct holdout dataset ID path + fixed step labels"
      contains: "per_agent_datasets"
  key_links:
    - from: "orq-agent/agents/tester.md"
      to: "orq-agent/templates/test-results.json"
      via: "tester writes per-split dataset IDs to per_agent_datasets[] entries"
      pattern: "holdout_dataset_id"
    - from: "orq-agent/agents/iterator.md"
      to: "orq-agent/templates/test-results.json"
      via: "iterator reads holdout_dataset_id from per_agent_datasets[] array"
      pattern: "per_agent_datasets.*holdout_dataset_id"
---

<objective>
Fix the holdout dataset ID path mismatch between test-results.json template and iterator.md, and fix stale step labels in iterator.md Phase 9.

Purpose: The re-test-on-holdout flow (ITER-05) is broken because iterator.md reads holdout dataset IDs from a path (`dataset.per_agent.{agent_key}.holdout_dataset_id`) that does not exist in the test-results.json template. This plan aligns the data contract across all three files and fixes cosmetic step label issues.

Output: Three files updated with consistent per-split dataset ID fields and correct step numbering.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-fix-holdout-dataset-path/10-RESEARCH.md
@orq-agent/templates/test-results.json
@orq-agent/agents/tester.md
@orq-agent/agents/iterator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-split dataset ID fields to template and tester</name>
  <files>orq-agent/templates/test-results.json, orq-agent/agents/tester.md</files>
  <action>
**File 1: `orq-agent/templates/test-results.json`**

In the `per_agent_datasets[]` entry (around line 19-26), add three new fields after `"dataset_id": "{{PLATFORM_DATASET_ID}}"` and before `"split_counts"`:

```json
"train_dataset_id": "{{TRAIN_DATASET_ID}}",
"test_dataset_id": "{{TEST_DATASET_ID}}",
"holdout_dataset_id": "{{HOLDOUT_DATASET_ID}}",
```

Keep the existing `"dataset_id"` field for backward compatibility. Do NOT remove it.

**File 2: `orq-agent/agents/tester.md`**

Two changes:

(a) **Phase 5.3 (around line 320-329):** After the existing per-split ID listing block, add an explicit instruction paragraph:

```
Write these per-split dataset IDs into the `per_agent_datasets[]` entry for each agent in `test-results.json`. Each entry must include `train_dataset_id`, `test_dataset_id`, and `holdout_dataset_id` fields alongside the existing `dataset_id` field.
```

(b) **Output Format section (around line 719):** After the line `"dataset_id": "{{PLATFORM_DATASET_ID}}",` add three new fields:

```json
"train_dataset_id": "{{TRAIN_DATASET_ID}}",
"test_dataset_id": "{{TEST_DATASET_ID}}",
"holdout_dataset_id": "{{HOLDOUT_DATASET_ID}}",
```

Keep the existing `"dataset_id"` line. Do NOT remove it.
  </action>
  <verify>
    <automated>grep -c "holdout_dataset_id" orq-agent/templates/test-results.json && grep -c "holdout_dataset_id" orq-agent/agents/tester.md && grep -c "train_dataset_id" orq-agent/templates/test-results.json && grep -c "test_dataset_id" orq-agent/templates/test-results.json</automated>
  </verify>
  <done>
- test-results.json per_agent_datasets[] has all four dataset ID fields (dataset_id, train_dataset_id, test_dataset_id, holdout_dataset_id)
- tester.md Phase 5.3 explicitly instructs writing per-split IDs to test-results.json
- tester.md Output Format includes per-split dataset ID fields
- Existing dataset_id field preserved in both files
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix iterator holdout path references and stale step labels</name>
  <files>orq-agent/agents/iterator.md</files>
  <action>
**Three changes in `orq-agent/agents/iterator.md`:**

(a) **Fix holdout dataset ID path -- TWO occurrences (lines 311 and 318):**

Both lines currently reference: `dataset.per_agent.{agent_key}.holdout_dataset_id`

Replace BOTH occurrences with the correct array-based lookup description. The new text should describe finding the matching entry in `dataset.per_agent_datasets[]` where `agent_key` matches the target agent, then reading `holdout_dataset_id` from that entry.

Line 311 (in Step 7.1, item 3): Change to something like:
`Dataset split override: "holdout" -- direct the tester to use holdout dataset IDs from test-results.json by finding the matching entry in dataset.per_agent_datasets[] (where agent_key matches) and reading its holdout_dataset_id field, instead of using the test split IDs`

Line 318 (in Step 7.2): Change to something like:
`The holdout dataset IDs are stored in test-results.json in the dataset.per_agent_datasets[] array -- find the entry where agent_key matches the target agent and read its holdout_dataset_id field`

IMPORTANT: Search for ALL occurrences of the old path pattern `dataset.per_agent.{agent_key}` to ensure none are missed. There should be exactly 2.

(b) **Add backward compatibility warning in Step 7.2 (after line 319):**

Add a paragraph after the holdout path description:

```
**Backward compatibility:** If the matched per_agent_datasets[] entry does not have a holdout_dataset_id field (old test-results.json format), warn the user: "Holdout dataset IDs not found in test-results.json. Re-run /orq-agent:test to generate updated results with per-split dataset IDs." Do not silently fail or skip the re-test.
```

(c) **Fix stale step labels in Phase 9 section -- THREE occurrences:**

These are in the "Phase 9: Logging and Audit Trail" section. Only change labels WITHIN the Phase 9 section. Do NOT touch any Phase 7 step labels (those are correct).

- Line 435: Change `### Step 7.1: iteration-log.md (Per Cycle)` to `### Step 9.1: iteration-log.md (Per Cycle)`
- Line 472: Change `### Step 7.2: audit-trail.md (Append-Only)` to `### Step 9.2: audit-trail.md (Append-Only)`
- Line 489: Change `### Step 7.3: Log Write Safety` to `### Step 9.3: Log Write Safety`

**Post-edit verification:** Confirm Phase 7 still has its original Step 7.1 through Step 7.5 labels intact. The stale labels were only in Phase 9.
  </action>
  <verify>
    <automated>grep -n "dataset.per_agent\.\{agent_key\}" orq-agent/agents/iterator.md | wc -l | tr -d ' ' && grep -n "per_agent_datasets" orq-agent/agents/iterator.md | head -5 && grep -n "Step 7\." orq-agent/agents/iterator.md | grep -i "phase 9" | wc -l | tr -d ' ' && grep -n "Step 9\." orq-agent/agents/iterator.md | head -5</automated>
  </verify>
  <done>
- Zero occurrences of old path `dataset.per_agent.{agent_key}.holdout_dataset_id` remain in iterator.md
- Both holdout path references use `per_agent_datasets[]` array lookup description
- Backward compatibility warning exists in Step 7.2
- Phase 9 step labels read Step 9.1, Step 9.2, Step 9.3
- Phase 7 step labels (Step 7.1 through Step 7.5) remain unchanged
  </done>
</task>

</tasks>

<verification>
1. `grep "holdout_dataset_id" orq-agent/templates/test-results.json` -- returns at least 1 match
2. `grep "train_dataset_id" orq-agent/templates/test-results.json` -- returns at least 1 match
3. `grep "test_dataset_id" orq-agent/templates/test-results.json` -- returns at least 1 match
4. `grep "dataset_id" orq-agent/templates/test-results.json` -- returns at least 2 matches (original + per-split)
5. `grep "holdout_dataset_id" orq-agent/agents/tester.md` -- returns matches in Phase 5.3 and Output Format
6. `grep "per_agent_datasets" orq-agent/agents/iterator.md` -- returns matches at both path references
7. `grep "dataset.per_agent.{agent_key}" orq-agent/agents/iterator.md` -- returns ZERO matches (old path removed)
8. `grep "Step 9\." orq-agent/agents/iterator.md` -- returns 3 matches (9.1, 9.2, 9.3)
9. `grep "Step 7\." orq-agent/agents/iterator.md` -- returns matches only in Phase 7 section (not Phase 9)
10. JSON validation: `python3 -c "import json; json.load(open('orq-agent/templates/test-results.json'))"` succeeds
</verification>

<success_criteria>
- The data contract (test-results.json) includes per-split dataset ID fields in per_agent_datasets[] entries
- The writer (tester.md) is instructed to populate per-split fields and its Output Format reflects them
- The reader (iterator.md) uses the correct array-based path to find holdout dataset IDs
- Old test-results.json files are handled gracefully with a warning (not silent failure)
- Phase 9 step labels are corrected without affecting Phase 7 step labels
- test-results.json remains valid JSON
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-holdout-dataset-path/10-01-SUMMARY.md`
</output>
