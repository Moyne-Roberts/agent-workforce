---
phase: 37-hitl-approval
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - web/lib/pipeline/__tests__/approval.test.ts
  - web/components/approval/__tests__/diff-viewer.test.ts
  - web/lib/pipeline/__tests__/approval-action.test.ts
  - web/lib/inngest/__tests__/pipeline-approval.test.ts
  - web/lib/email/__tests__/approval-notification.test.ts
  - web/lib/pipeline/__tests__/approval-audit.test.ts
  - web/package.json
autonomous: true
requirements: [HITL-01, HITL-02, HITL-03, HITL-04, HITL-05, HITL-06]

must_haves:
  truths:
    - "All 6 test stub files exist with it.todo() behavioral contracts"
    - "react-diff-viewer-continued and resend npm packages are installed"
    - "Existing tests still pass after adding new stubs"
  artifacts:
    - path: "web/lib/pipeline/__tests__/approval.test.ts"
      provides: "Test stubs for HITL-01 approval creation logic"
      contains: "it.todo"
    - path: "web/components/approval/__tests__/diff-viewer.test.ts"
      provides: "Test stubs for HITL-02 diff viewer rendering"
      contains: "it.todo"
    - path: "web/lib/pipeline/__tests__/approval-action.test.ts"
      provides: "Test stubs for HITL-03 server action logic"
      contains: "it.todo"
    - path: "web/lib/inngest/__tests__/pipeline-approval.test.ts"
      provides: "Test stubs for HITL-04 pipeline resume logic"
      contains: "it.todo"
    - path: "web/lib/email/__tests__/approval-notification.test.ts"
      provides: "Test stubs for HITL-05 email sending"
      contains: "it.todo"
    - path: "web/lib/pipeline/__tests__/approval-audit.test.ts"
      provides: "Test stubs for HITL-06 audit trail"
      contains: "it.todo"
  key_links: []
---

<objective>
Install npm dependencies and create Wave 0 test stub files for all Phase 37 requirements.

Purpose: Establish Nyquist-compliant test infrastructure so that subsequent plans have automated verification commands from the start.
Output: 6 test stub files with it.todo() behavioral contracts, react-diff-viewer-continued and resend installed.
</objective>

<execution_context>
@/Users/nickcrutzen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nickcrutzen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-hitl-approval/37-RESEARCH.md
@.planning/phases/37-hitl-approval/37-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm dependencies and create test stubs</name>
  <files>
    web/package.json,
    web/lib/pipeline/__tests__/approval.test.ts,
    web/components/approval/__tests__/diff-viewer.test.ts,
    web/lib/pipeline/__tests__/approval-action.test.ts,
    web/lib/inngest/__tests__/pipeline-approval.test.ts,
    web/lib/email/__tests__/approval-notification.test.ts,
    web/lib/pipeline/__tests__/approval-audit.test.ts
  </files>
  <read_first>
    web/package.json,
    web/vitest.config.ts,
    web/lib/pipeline/__tests__/stages.test.ts,
    .planning/phases/37-hitl-approval/37-VALIDATION.md,
    .planning/phases/37-hitl-approval/37-RESEARCH.md
  </read_first>
  <action>
1. Install new npm packages:
   ```
   cd web && npm install react-diff-viewer-continued resend
   ```

2. Create `web/lib/pipeline/__tests__/approval.test.ts` with these it.todo() stubs:
   - describe("createApprovalRequest")
     - it.todo("inserts a row into approval_requests with status pending")
     - it.todo("stores old_content and new_content in the row")
     - it.todo("stores the plain-English explanation in the row")
     - it.todo("returns the created approval ID")

3. Create `web/components/approval/__tests__/diff-viewer.test.ts` with:
   - describe("ApprovalDiffViewer")
     - it.todo("renders with oldContent and newContent props")
     - it.todo("displays 'Current Prompt' as left title")
     - it.todo("displays 'Proposed Changes' as right title")
     - it.todo("supports splitView toggle")

4. Create `web/lib/pipeline/__tests__/approval-action.test.ts` with:
   - describe("submitApprovalDecision")
     - it.todo("updates approval_requests row with decision and decided_by")
     - it.todo("sends pipeline/approval.decided Inngest event")
     - it.todo("prevents double-submit by checking status is pending")
     - it.todo("throws if user is not authenticated")

5. Create `web/lib/inngest/__tests__/pipeline-approval.test.ts` with:
   - describe("pipeline HITL integration")
     - it.todo("creates approval request before calling waitForEvent")
     - it.todo("resumes pipeline when approval.decided event received with approved")
     - it.todo("skips proposed change when decision is rejected")
     - it.todo("throws on 7-day timeout")

6. Create `web/lib/email/__tests__/approval-notification.test.ts` with:
   - describe("sendApprovalEmail")
     - it.todo("calls resend.emails.send with correct recipient")
     - it.todo("includes project name and step name in subject")
     - it.todo("includes approval URL with approvalId query param")
     - it.todo("does not throw when Resend API fails (best-effort)")

7. Create `web/lib/pipeline/__tests__/approval-audit.test.ts` with:
   - describe("approval audit trail")
     - it.todo("stores decided_by user ID on approval decision")
     - it.todo("stores decided_at timestamp on approval decision")
     - it.todo("stores comment text when provided")
     - it.todo("stores null comment when not provided")

Each file should import only from vitest: `import { describe, it } from "vitest";`
Do NOT import from source files that do not exist yet -- use only it.todo() (not it.skip()).
Follow the exact pattern from web/lib/pipeline/__tests__/stages.test.ts but with it.todo() instead of full test bodies.
  </action>
  <verify>
    <automated>cd /Users/nickcrutzen/Developer/agent-workforce/web && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - web/lib/pipeline/__tests__/approval.test.ts contains "it.todo" (at least 4 stubs)
    - web/components/approval/__tests__/diff-viewer.test.ts contains "it.todo" (at least 4 stubs)
    - web/lib/pipeline/__tests__/approval-action.test.ts contains "it.todo" (at least 4 stubs)
    - web/lib/inngest/__tests__/pipeline-approval.test.ts contains "it.todo" (at least 4 stubs)
    - web/lib/email/__tests__/approval-notification.test.ts contains "it.todo" (at least 4 stubs)
    - web/lib/pipeline/__tests__/approval-audit.test.ts contains "it.todo" (at least 4 stubs)
    - web/package.json contains "react-diff-viewer-continued"
    - web/package.json contains "resend"
    - `cd web && npx vitest run` exits with code 0
  </acceptance_criteria>
  <done>All 6 test stub files created with it.todo() behavioral contracts, react-diff-viewer-continued and resend installed, existing tests still pass.</done>
</task>

</tasks>

<verification>
cd /Users/nickcrutzen/Developer/agent-workforce/web && npx vitest run --reporter=verbose
</verification>

<success_criteria>
- 6 new test files with it.todo() stubs exist
- react-diff-viewer-continued and resend are in package.json dependencies
- Full test suite passes (including existing Phase 35/36 tests)
</success_criteria>

<output>
After completion, create `.planning/phases/37-hitl-approval/37-00-SUMMARY.md`
</output>
